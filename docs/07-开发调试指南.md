<!--
 * @Author: lzx 1245634367@qq.com
 * @Date: 2026-08-03 23:23:58
 * @LastEditors: lzx 1245634367@qq.com
 * @LastEditTime: 2026-08-03 23:23:58
 * @FilePath: \feishu-work\docs\07-开发调试指南.md
 * @Description: Fuck Bug
 * 微信：lizx2066
-->
# 07 · 开发调试指南

> 适用场景：本地/测试环境开发与调试「飞书报工系统」。
> 关联文档：[04-飞书对接方案](./04-飞书对接方案.md)、[06-生产部署指南](./06-生产部署指南.md)

## 一、本地开发环境

| 项 | 要求 |
|---|---|
| Node.js | ≥ 18 LTS（建议 20） |
| MySQL | 8.x，本地或远程（如阿里云 RDS）均可 |
| 飞书 | 自建应用（可先在沙箱/测试租户调试） |

目录结构：

```
feishu-work/
├── server/     # 后端 NestJS（端口 3000）
└── web/        # 前端 Vite（端口 5173，/api 代理到 3000）
```

## 二、初始化数据库

```bash
cd server
npm install
cp .env.example .env        # 按需修改 DATABASE_URL 等

# 直接同步表结构（无需影子库）
npx prisma db push
# 可选：查看表数据
npx prisma studio
```

> 若用 `prisma migrate dev` 报 `P3014`：需要「创建影子库」权限，详见本文档第八节常见问题。

## 三、启动后端

```bash
cd server
npm run start:dev        # 热重载（watch 模式）
# 启动后: http://localhost:3000/api
```

验证健康：

```bash
curl http://localhost:3000/api/auth/me        # 未带 token → 401（说明服务正常）
curl http://localhost:3000/api/calendar/is-holiday?date=2026-08-03
```

## 四、启动前端

```bash
cd web
npm install
cp .env.example .env      # 填 VITE_FEISHU_APP_ID
npm run dev               # http://localhost:5173（/api 自动代理到 3000）
```

> 前端通过 Vite 代理请求后端，无需配置跨域（生产走 Nginx 反代）。

## 五、登录/免登调试（重点）

### 5.1 原理回顾

- 端内免登：前端调 `tt.requestAccess` / `tt.requestAuthCode` 拿到 `code` → 后端换 `user_access_token` → 取 `open_id` → 签发 JWT。
- **这些 JSAPI 只在飞书客户端内可用**。用普通浏览器直接打开页面时没有 `tt`，会提示「不支持免登」。

### 5.2 调试方案 A：在飞书客户端内调试（走真实免登）

1. 开发阶段把后端/前端暴露到公网（见第六节 ngrok），或临时把飞书「网页应用主页地址」指向本地/内网地址；
2. 在**飞书开发者工具**中添加网页项目并填入本地地址，用移动端二维码在飞书内打开（详见 `docs/feishu/步骤四`）；
3. 飞书内打开页面 → 触发免登 → 正常进入系统。

### 5.3 调试方案 B：临时开发登录接口（浏览器调试推荐）

浏览器环境下无法免登，但大部分业务功能（项目/报工/日历）都需要登录态。建议在**仅开发环境**加一个调试登录接口（生产必须关闭）：

```typescript
// server/src/modules/auth/auth.controller.ts 中追加
// .env 增加 DEV_LOGIN_ENABLED=1（生产不设置）
@Post('dev-login')
devLogin(@Body() body: { openId: string }) {
  if (process.env.DEV_LOGIN_ENABLED !== '1') {
    throw new ForbiddenException('开发登录未开启');
  }
  return this.authService.loginByOpenId(body.openId); // 用 openId 直接签发 JWT
}
```

在 `auth.service.ts` 增加（复用现有逻辑）：

```typescript
async loginByOpenId(openId: string) {
  const admin = await this.prisma.admin.findUnique({ where: { openId } });
  const token = this.jwt.sign({ sub: openId, isAdmin: !!admin });
  const cached = await this.prisma.feishuUserCache.findUnique({ where: { openId } });
  return { token, user: { openId, name: cached?.name || openId, isAdmin: !!admin } };
}
```

然后浏览器里先 `POST /api/auth/dev-login { "openId": "你的open_id" }` 拿到 token 存 localStorage 即可调试。

### 5.4 调试方案 C：手工构造 JWT（最快）

```bash
# 用与后端一致的 JWT_SECRET 生成
node -e "const j=require('jsonwebtoken');console.log(j.sign({sub:'ou_xxx',name:'张三',isAdmin:true},'你的JWT_SECRET',{expiresIn:'7d'}))"
```

把输出的 token 粘贴到浏览器 `localStorage.setItem('token', '<token>')` 后刷新页面。

> 需 `npm i -D jsonwebtoken` 或在后端临时脚本生成。

## 六、公网/回调调试

飞书的**事件回调**必须公网 HTTPS。本地开发调试审批回调时用内网穿透：

### 6.1 ngrok（推荐）

```bash
# 后端 + 前端都代理
ngrok http 3000 --host-header=rewrite
# 得到 https://xxxx.ngrok-free.app
```

- 飞书后台「事件订阅」请求地址临时填：`https://xxxx.ngrok-free.app/api/internal/feishu/approval-callback`
- 网页应用主页可临时指向 `https://xxxx.ngrok-free.app`（前端需把 `/api` 也走同域，Vite 已代理，但 ngrok 直接暴露 3000 时静态资源需由后端/前端 dev server 提供，建议同时 `ngrok http 5173` 并让前端代理指向穿透域名）

### 6.2 本地回调自测（不经飞书）

直接向回调地址 POST 一个模拟事件，验证状态回写逻辑：

```bash
curl -X POST http://localhost:3000/api/internal/feishu/approval-callback \
  -H "Content-Type: application/json" \
  -d '{"event":{"instance_id":"xxxx","instance_code":"xxxx"}}'
```

### 6.3 事件回调加密

若在飞书后台开启了 **Encrypt Key**，回调体为 `{"encrypt":"..."}`，需要解密后再解析（当前后端对加密事件返回占位，需接入解密）。

- 官方 Node SDK `@larksuiteoapi/node-sdk` 内置解密；
- 或自实现 AES-CBC：密钥为 Encrypt Key 的 32 字节，IV 取密文前 16 字节，明文含 16 字节随机前缀后才是 JSON。

## 七、各模块调试步骤

### 7.1 通讯录 / 用户

```bash
# 同步通讯录（管理员）
curl -X POST http://localhost:3000/api/users/sync -H "Authorization: Bearer <token>"
# 用户列表
curl "http://localhost:3000/api/users?keyword=张" -H "Authorization: Bearer <token>"
```

### 7.2 项目

```bash
# 创建（管理员）
curl -X POST http://localhost:3000/api/projects \
  -H "Authorization: Bearer <token>" -H "Content-Type: application/json" \
  -d '{"name":"测试项目","ownerOpenIds":["ou_xxx"]}'
# 列表
curl "http://localhost:3000/api/projects" -H "Authorization: Bearer <token>"
```

### 7.3 报工（核心规则）

| 场景 | 构造数据 | 预期 |
|---|---|---|
| 工作日 ≤8h 无加班 | `reportDate=工作日, normalHours=8, overtimeHours=0` | 免审批，`status=2 已通过` |
| 工作日 加班>0 | `normalHours=8, overtimeHours=2` | `status=1 审批中`，创建飞书审批 |
| 工作日 普通>8h | `normalHours=9, overtimeHours=0` | **被拦截**，提示填加班时长 |
| 节假日 | 先配日历例外，再报工 | 一律 `status=1 审批中` |
| 重复报工 | 同项目同日再报 | 被拦截「已报工」 |

```bash
curl -X POST http://localhost:3000/api/reports \
  -H "Authorization: Bearer <token>" -H "Content-Type: application/json" \
  -d '{"projectId":1,"reportDate":"2026-08-03","normalHours":8,"overtimeHours":0}'
```

### 7.4 审批回写

1. 提交一条加班报工 → 在飞书审批中心处理（通过/驳回）；
2. 观察后端日志：事件回调触发 → 状态更新；
3. 若未触发回调（如本地没公网），等待定时任务「审批对账」（每 5 分钟）主动同步，或手动：
   ```bash
   curl -X POST http://localhost:3000/api/internal/approvals/reconcile -H "Authorization: Bearer <token>"
   ```

### 7.5 消息通知

提交流程后，查看 `message_log` 表确认发送状态；若发送失败，检查应用可用范围与 `im:message` 权限。

## 八、常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 浏览器打开提示「不支持免登」 | 免登 JSAPI 仅飞书客户端可用 | 用方案 A/B/C 调试 |
| 免登 code 换 token 失败 | AppID/Secret 错误，或应用未发布 | 核对 `.env`；确认版本已发布 |
| 登录后 403 | 非管理员访问管理接口 | 确认 `admin` 表已加自己 openId |
| 审批实例创建失败 | `FEISHU_APPROVAL_CODE` 不对 / 无 `approval:approval` 权限 | 核对审批定义编码与权限 |
| 审批人收不到卡片 | 应用可用范围不含审批人 / `im:message` 未开通 | 后台调整范围与权限 |
| 事件回调一直失败 | 回调地址非公网 HTTPS / 未订阅 `approval_instance` / 未应答 `challenge` | 用 ngrok + 订阅事件 + 后端已实现 challenge |
| 审批状态没回写 | 回调没进来 | 手动 reconcile 对账；检查事件订阅地址 |
| `prisma migrate dev` 报 P3014/P1010 | 无创建影子库权限 | 用 `prisma db push`；或配 `shadowDatabaseUrl`（见 06 文档） |
| `im:message` 消息超频 | 应用消息频控 | 失败落 `message_log`，实现重试/退避 |
| H5 可信域名报错 333447/333448 | 调用 JSAPI 需配置可信域名 | 本系统主要走服务端 API，一般无需 JSAPI；如需则后台配置 |

## 九、命令速查

```bash
# 后端
cd server
npm run start:dev        # 开发热重载
npx prisma db push       # 同步表结构
npx prisma studio        # 可视化查看数据
npx prisma migrate dev   # 生成迁移（需影子库权限）
npm run build            # 编译

# 前端
cd web
npm run dev              # 开发服务器 5173
npm run build            # 构建 dist

# 进程
pm2 logs feishu-work-server
```

## 十、调试小技巧

- 后端日志会打印飞书 API 调用与失败原因（`FeishuService` logger），排查飞书接口问题时先看它。
- `prisma studio` 可快速造数/看数，非常适合验证报工规则与审批状态。
- 报工规则（8h/加班/节假日）集中在 `server/src/modules/report/report.service.ts`，改规则只动这一处。
- 节假日判定在 `server/src/modules/calendar/calendar.service.ts`，默认周末为节假日，`calendar_rule` 表存例外。
- 前端主题在 `web/src/styles/theme.css`，页面风格遵循 `docs/DESIGN.md`。
